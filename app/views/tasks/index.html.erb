<h1>My tasks</h1>
<ul>
  <% @tasks.each do |t| %>
    <li><%= t.completed ? '[X]  ' : '[ ]' %><%= link_to t.title, task_path(t) %><%= link_to '🗑', task_path(t), data: { turbo_method: :delete, turbo_confirm: 'Really?' } %></li>
  <% end %>
</ul>
<p><%= link_to 'add a new task', new_task_path %></p>
