<h1>My tasks</h1>
<ul>
  <% @tasks.each do |t| %>
    <li><%= t.completed ? '[X]  ' : '[ ]' %><%= link_to t.title, task_path(t) %></li>
  <% end %>
</ul>
<%= link_to 'add a new task', new_task_path %>
